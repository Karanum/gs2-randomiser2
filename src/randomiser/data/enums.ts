/**
 * Represents the in-game elements, including physical/non-elemental.
 */
export enum Element {
    VENUS = 0,
    MERCURY,
    MARS,
    JUPITER,
    PHYSICAL
}

/**
 * Represents the stat blocks for characters and enemies in the game data.
 */
export enum StatBlock {
    HP = 0,
    PP,
    ATTACK,
    DEFENSE,
    AGILITY,
    LUCK
}