/**
 * Represents the different display methods for world map locations.
 * When on the Teleport map, `BLUE` will place the player on the ship.
 */
export enum WorldMapDisplay {
    NONE,
    YELLOW,
    BLUE
}